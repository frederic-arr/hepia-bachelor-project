[package]
name = "isolation-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[lints]
workspace = true

[dependencies]
quote = { workspace = true, features = ["proc-macro"] }
syn = { workspace = true, features = ["proc-macro", "parsing"] }

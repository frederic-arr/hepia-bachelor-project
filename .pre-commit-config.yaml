---
repos:
  - repo: local
    hooks:
      - id: just-pre-commit
        name: Pre-commit Hook
        language: system
        entry: just _pre-commit
        pass_filenames: false
        always_run: true
